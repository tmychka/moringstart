# Backend origin for API requests.
# Leave empty in development — requests are proxied to http://localhost:3000 by Vite
# (see vite.config.js). In production, set this to your deployed backend origin, e.g.:
# VITE_API_URL=https://api.example.com
VITE_API_URL=
